#include<stdio.h>
int main(){
    int in;
    scanf("%d",&in);
    if(in==0){
        printf("It contains 1 digits.");
        return 0;
    }
    printf("It contains %d digits.",(int)log10(abs(in))+1);
    return 0;
}
