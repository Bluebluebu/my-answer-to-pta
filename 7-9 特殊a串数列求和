#include<stdio.h>
#include<math.h>
int main(){
    int a,n;
    scanf("%d %d",&a,&n);
    int sum=0;
    for(int i=1;i<=n;i++){
        int plus=0;
        for(int j=0;j<i;j++){
            plus+=a*pow(10,j);
        }sum+=plus;
    }
    printf("s = %d",sum);
    return 0;
}
